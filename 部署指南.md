# 📱 加密版部署指南（网址 + APK）

## 一、发布包内容
`发布-加密版/` 文件夹内含：
- `index.html` —— **加密混淆后的主程序**（主逻辑+密码逻辑已混淆，源码不可直接阅读）
- `klinecharts.min.js` —— K线库本地文件（不依赖外网 CDN，离线可用）
- `manifest.webmanifest` + `icons/` —— PWA 清单与图标（用于安装/转 APK）
- `sw.js` —— 离线缓存 Service Worker

## 二、访问密码说明（重要）
- 默认访问密码 = `Aa592043278.`（管理密码仍为 `Aa4483727.`）
- 部署后给别人用：对方打开网址输入访问密码即可，10 天内免重复输入
- 想改访问密码：页面右上角 **🔑** → 输入管理密码 `Aa4483727.`（保持不变） → 设置新访问密码
- ⚠️ 安全提醒：默认访问密码已改为你的新密码 `Aa592043278.`；管理密码 `Aa4483727.` 保持不变；客户端密码仅防普通使用者，技术高手仍可通过浏览器开发者工具绕过（纯前端限制，如需更强安全需加服务器鉴权）

## 三、做成网址（免费，二选一）

### 方式 A：GitHub Pages（你有 github 账号，推荐）
1. 打开 https://github.com/cairui5920/index-html-demo（或新建仓库）
2. 点 **Add file → Upload files**，把 `发布-加密版` 里的文件全部拖进去（含 index.html、klinecharts.min.js、manifest.webmanifest、sw.js、icons 文件夹）
3. Commit 提交
4. 仓库 **Settings → Pages → Source** 选 `main` 分支 / root 目录 → Save
5. 等 1-2 分钟，网址即：`https://cairui5920.github.io/index-html-demo/`
6. 提示：klinecharts.min.js、icons 必须与 index.html 同级放，不要只传一个 html

### 方式 B：Netlify（免费静态托管）
1. 打开 https://app.netlify.com/drop
2. 把 `发布-加密版` 整个文件夹拖进页面
3. 几秒后生成随机网址，可自定义名称：Site settings → Change site name（如 `heyu-radar` → `https://heyu-radar.netlify.app`）

## 四、转成 APK（自己手机用，免费）
> 前提：先把网址部署好（方式 A 或 B），APK 由线上工具从网址生成

### 方法：PWABuilder（推荐，免安装）
1. 打开 https://www.pwabuilder.com
2. 输入你的网址（如 GitHub Pages 那个），点 Start
3. 稍等检测，点 **Package for stores**（或 Android 包）
4. 选 **Android** → 点 **Generate package**（首次需登录微软账号，免费）
5. 下载生成的 zip，解压，把里面的 **`.aab`** 发给手机或用 Android Studio 签名转 APK；
   或直接在手机上用 Chrome 打开网址 → 菜单「添加到主屏幕」当 App 用（无需 APK）

### 方法二：手机直接用（最省事）
- 手机 Chrome/Safari 打开网址 → 「添加到主屏幕」→ 桌面上生成图标，全屏 App 体验
- 配合 PWA 清单，接近原生 App

### 方法三：WebView 打包（可选进阶）
- 用 Android Studio 建一个 WebView 项目，`webView.loadUrl("你的网址")`，签名出 APK；网上有大量「Website 2 APK」在线工具也可直接输入网址生成

## 五、安全性说明（诚实告知）
- **已升级的安全措施（本次）**：
  1. **PBKDF2-SHA256 强哈希**（10 万次迭代 + 随机盐）：密码不再用可瞬间破解的简易哈希，离线暴力破解成本大幅提升；
  2. **失败锁定**：连续 5 次错误密码自动锁定 30 秒，防在线爆破；
  3. **代码混淆**：主程序 + 密码逻辑混淆，源码不可直接阅读；K 线库本地化、离线可用；
  4. **访问密码默认值已改为你的新密码**（管理密码 Aa4483727. 不变，用于改密）。
- **边界说明**：纯前端应用无法做到"绝对不可破解"——浏览器必须执行代码，技术高手仍可逆向/绕过；
- 如需更高安全：① 部署到 HTTPS（GitHub Pages/Netlify 自带）；② 敏感数据建议后端加鉴权/限流。


## 六、更新流程
改完功能后：重新运行「加密版构建」，替换 `发布-加密版/index.html`，重新上传到 GitHub/Netlify 即可；PWA 会自动更新（Service Worker 缓存 1 版）。
## 七、一键发布脚本（本机）
工作目录已提供一键发布工具（无需记命令）：
- 双击 **`发布.bat`** 即可完成：加密构建 -> 刷新 github-pages -> git 提交推送 -> 检查 Pages -> 验证网址。
- 底层脚本 `publish.ps1`（可手动运行）；令牌保存在 `.gh_token`（30 天有效，过期后到 https://github.com/settings/tokens 重新生成并覆盖该文件）。
- 当前线上网址：**https://cairui5920.github.io/index-html-demo/**
- 更新流程：改完主程序 `新版监视K线工具-免VPN版.html` -> 双击 `发布.bat` -> 约 1 分钟线上生效。